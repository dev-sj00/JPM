plugins {
    id 'java-library'
    id 'maven-publish'
    id 'java-gradle-plugin'
}

group = 'com.github.dev_sj'
version = '1.0.0'

gradlePlugin {
    plugins {
        jpmPlugin {
            id = 'com.github.dev_sj.jpm'
            implementationClass = 'config.plugin.JpmPlugin'
        }
    }
}



dependencies {
    // 🔹 JPM 자체 (JitPack에서 가져올 경우)
    implementation 'com.github.dev-sj00:JPM:Tag'

    // 🔹 MyBatis Core
    implementation 'org.mybatis:mybatis:3.5.16'

    // 🔹 (선택) MyBatis-Spring 연동용
    // implementation 'org.mybatis.spring:mybatis-spring:3.0.3'

    // 🔹 (선택) JDBC 드라이버 예시
    // runtimeOnly 'org.postgresql:postgresql:42.7.2'
    // runtimeOnly 'mysql:mysql-connector-java:8.3.0'
}

publishing {
    publications {
        mavenJava(MavenPublication) {
            from components.java
        }
    }
}
